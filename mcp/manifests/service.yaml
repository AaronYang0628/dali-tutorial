apiVersion: v1
kind: Service
metadata:
  name: dali-mcp-service
  labels:
    app: dali-mcp-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: dali-mcp-service
