apiVersion: v1
kind: Service
metadata:
  name: dali-mcp-server
  labels:
    app: dali-mcp-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: dali-mcp-server
