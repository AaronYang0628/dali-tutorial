# DALI MCP Server Requirements

# MCP SDK
mcp>=1.0.0

# NVIDIA DALI - GPU accelerated data loading library
# For CUDA 12.x (use nvidia-dali-cuda110 for CUDA 11.x)
--extra-index-url https://pypi.nvidia.com
nvidia-dali-cuda120>=1.30.0

# Core dependencies
numpy>=1.20.0
pillow>=9.0.0

# S3 and cloud storage support
boto3>=1.26.0
botocore>=1.29.0

# HTTP/SSE Server dependencies (for dali_mcp_server_http.py)
starlette>=0.27.0
uvicorn[standard]>=0.24.0
sse-starlette>=1.6.0

# Optional dependencies
# torch>=2.0.0  # For PyTorch integration
# opencv-python>=4.5.0  # For advanced image processing
